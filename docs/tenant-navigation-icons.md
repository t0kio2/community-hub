# テナントナビゲーションアイコン

サイドバーの業務領域を文字記号ではなく、同じ線幅・サイズのSVGアイコンで識別する。

- 求人管理：ブリーフケース
- 宿泊管理：ベッド
- 組織設定：オフィスビル
- ホーム：家

ホームを含むすべてのアイコンを同じ線画SVGへ統一する。領域アイコンは30pxの背景ボックス内に18px、ホームアイコンは20pxで表示する。アイコンは見出しの補助情報なので`aria-hidden="true"`とし、領域名は既存のテキスト見出しで読み上げる。SVGは`currentColor`を使用し、通常・アクティブ状態の配色をCSSから制御する。
