<% content_for :title, "求人編集" %>
<% content_for :page_title, "求人を編集" %>
<% content_for :stylesheets do %><%= stylesheet_link_tag "tenant/listings", "data-turbo-track": "reload" %><% end %>
<% content_for :back_link do %><%= link_to "← 求人詳細へ戻る", tenant_job_path(@listing), class: "tenant-back-link" %><% end %>

<%= render "tenant/listings/form", listing: @listing, detail: @job_listing, form_url: tenant_job_path(@listing), separated_resource: true %>
