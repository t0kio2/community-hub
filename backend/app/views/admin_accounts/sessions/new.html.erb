<header class="authentication-card__header">
  <p>ADMIN SIGN IN</p>
  <h2>運営管理者ログイン</h2>
  <span>Community Hubの運営管理者アカウントでログインしてください。</span>
</header>

<%= form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: "authentication-form" }) do |f| %>
  <div class="authentication-field">
    <%= f.label :email, "メールアドレス" %>
    <%= f.email_field :email, id: "admin_email", required: true, autofocus: true, autocomplete: "username", placeholder: "admin@example.com" %>
  </div>

  <div class="authentication-field">
    <%= f.label :password, "パスワード" %>
    <%= f.password_field :password, id: "admin_password", required: true, autocomplete: "current-password", placeholder: "パスワードを入力" %>
  </div>

  <% if devise_mapping.rememberable? %>
    <label class="authentication-remember">
      <%= f.check_box :remember_me %>
      <span>ログイン状態を保持する</span>
    </label>
  <% end %>

  <%= f.submit "ログイン", class: "authentication-submit" %>
<% end %>

<p class="authentication-support">管理者アカウントに関する問題は、システム管理担当へお問い合わせください。</p>
