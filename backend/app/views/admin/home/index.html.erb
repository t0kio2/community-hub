<% content_for :title, "Admin Dashboard" %>

<!--
<section class="dashboard">
  <h1>Admin Dashboard</h1>
  <p>運営管理メニューからテナントアカウントと掲載情報を管理できます。</p>

  <div class="dashboard-actions">
    <a class="button" href="<%= admin_tenant_accounts_path %>">テナント一覧</a>
    <a class="button" href="<%= new_admin_tenant_account_path %>">テナントアカウントを作成</a>
  </div>
</section>
<% @articles.each do |article| %>
  <div style="border: 1px solid #ccc; padding: 15px; margin-bottom: 15px; border-radius: 8px;">
    <h3><%= article[:title] %></h3>

    <%= turbo_frame_tag "article_#{article[:id]}" do %>
    <% end %>
  </div>
<% end %>
-->

<style>
  .dashboard { max-width: 720px; padding: 24px; border: 1px solid #ddd; border-radius: 8px; }
  .dashboard h1 { margin-top: 0; }
  .dashboard-actions { display: flex; flex-wrap: wrap; gap: 8px; margin-top: 20px; }
</style>
